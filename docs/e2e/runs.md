# E2E 실행 기록

> append-only. 실행마다 아래 블록을 하나 더한다. 결과 코드: PASS / FAIL(→ findings ID) / SKIP(사유).

## 시나리오별 최근 통과 (실행 후 이 표만 갱신)

> 아래 블록들은 append-only 기록이고, 이 표가 "무엇을 언제 마지막으로 통과했나"의 정본이다. 코드가 그 흐름을 건드렸으면 **마지막 PASS 이전 커밋**이므로 다시 돌린다. 날짜·Week는 아래 블록 제목과 같다.

| 권 | 시나리오 | 마지막 PASS | 근거 블록 | 비고 |
|---|---|---|---|---|
| account | A0 미들웨어 경계 | 2026-09-16 Week 63 | ② A0.6 | |
| account | A1 이메일 가입 | 2026-09-16 Week 63 | ① A1 · ⑤ A1.12 · ⑥ A1.2 | |
| account | A2 로그인 | 2026-09-15 Week 62 | Phase 1 | SKIP 1 |
| account | A3 아이디 찾기 | 2026-09-15 Week 62 | Phase 1 | |
| account | A4 비밀번호 찾기 | 2026-09-15 Week 62 | Phase 1 | 운영자 문의 안내 |
| account | A5 온보딩 게이트 | 2026-09-15 Week 62 | Phase 1 | |
| account | A6 프로필 설정 | 2026-09-16 Week 63 | ① A6.6 · ⑥ A6.2·A6.3 | |
| account | A7 비밀번호 변경 | 2026-09-16 Week 63 | ⑤ A7.2 | |
| account | A8 구글 로그인 | 2026-09-16 Week 63 | ② A8.5 · M 행(구글 실측) | A8.6(같은 이메일 병합) 미실행 |
| account | A9 회원 탈퇴 | 2026-09-16 Week 63 | ⑤ A9.8 · A9.10 | |
| match-room | S0 준비 | 2026-09-15 Week 62 | Phase 2 | |
| match-room | S1 단식 기본 | 2026-09-16 Week 64 | S1.4·1.12a | |
| match-room | S2 이의·정정 | 2026-09-16 Week 63 | ⑤ S2.4·S2.6 | |
| match-room | S3 호스트 + 게스트 4 | 2026-09-16 Week 64 | S3.2(선수 입력) | |
| match-room | S4 복식 회원 4 | 2026-09-16 Week 63 | ③ S4.13 · ⑥ S4.2 | |
| match-room | S4b 혼합 복식 | 2026-09-15 Week 62 | Phase 2 | |
| match-room | S5 로테이션 빌더 | 2026-09-15 Week 62 | Phase 2 | |
| match-room | S6 강퇴·재초대·나가기 | 2026-09-16 Week 64 | S6.6·6.7 | |
| match-room | S7 비밀번호 입장·변경 | 2026-09-15 Week 62 | Phase 2 | |
| match-room | S8 목록·뱃지 항등식 | 2026-09-15 Week 62 | Phase 2 | S8.10(내 차례 우선 정렬) 미실행 |
| match-room | S9 직접 기록 | 2026-09-15 Week 62 | Phase 2 | |
| match-room | S10 권한·경계 | 2026-09-15 Week 62 | Phase 2 | |
| match-room | S11 단식 4명 자동 대진표 | 2026-09-15 Week 62 | Phase 2 | |
| match-room | S12 매칭 닫기·다시 열기 | 2026-09-15 Week 62 | Phase 2 | 첫 브라우저 실행 |
| match-room | S13 비노출 방 | 2026-09-16 Week 64 | S13.10 | |
| match-room | S14 협상 전이 전수 | 2026-09-15 Week 62 | Phase 2 | |
| match-room | S15 권한 매트릭스 | 2026-09-16 Week 64 | S15.8·15.9 | 15.8 21명 선택은 미실행 |
| record | R1 직접 기록 | 2026-09-16 Week 63 | ⑤ R1.2 | |
| record | R2 목록 그룹 | 2026-09-15 Week 62 | Phase 3 | |
| record | R3 통계 대조 | 2026-09-15 Week 62 | Phase 3 | |
| record | R4 프로필 | 2026-09-16 Week 63 | ④ R4.6 | |
| record | R5 셸·내비 | 2026-09-16 Week 67 | 사이드바 5항목 + CTA | |
| journey | J1 신규 회원 첫 확정 | 2026-09-15 Week 62 | Phase 4 | 관찰(클릭 카운트) |
| journey | J2 복식 정산까지 | 2026-09-15 Week 62 | Phase 4 | 관찰 |
| journey | J3 390px | 2026-09-15 Week 62 | Phase 4 | |
| 셸 | 랜딩 · 채움 버튼 · 환경 분리 | 2026-09-16 Week 65~68 | 각 블록 | 시나리오 밖 확인 |

## 2026-09-11 · Week 49 첫 전수 실행

- 커밋: `a21fd7e`
- 실행: Claude(Playwright MCP) + Supabase MCP SQL 보조
- 환경: 원격 Supabase, `npm run dev`, 뷰포트 1280×900
- 계정: A 남자01 · B 남자02 · C 남자03 · D 남자04

| 시나리오 | 결과 | 비고 |
|---|---|---|
| S0 | PASS | 정리 SQL 0건, 보호 경로 리다이렉트, 뱃지 0 |
| S1 | PASS | 22단계 전부. 1.22 기대값 수정(정산 방은 날짜 축에서도 `종료된 경기`). 발견 F-1·F-2·F-3·F-4·F-5 |
| S2 | PASS | 정정(reopen)은 B(이의자)가 수행해 좌석 누구나 가능함을 확인. 2.5 기대값 수정(maxLength 잘림). 2.12 A 확인·2.13은 SQL. 발견 F-6·F-7 |
| S3 | PASS | 20단계 전부. 0076 회귀(초기값=권장·회원 예외 경고·team1 첫 자리 회원·direct 3행) 확인. 게스트 빼기 뒤 파생 `비회원` 행 유지. 발견 F-8, 기지 K-6 confirmed. 정산된 방에 [게임 추가] 잔존(F-5 재확인) |
| S4 | PASS(기대 수정 1) | 4.1~4.14. 4.9 좌석은 회원 넷이라 `n/4명 확인`, D까지 확인해야 확정. 4.12 화면([내보내기] 없음)·RPC(`member_has_games`) 짝 확인. 4.13 기대 수정 — 게임 전부 확정 뒤에도 세션이 남아 미정산(F-11 발견, K-12 confirmed). B·C 확인·게임 2 전체는 SQL. 발견 F-9·F-10·F-11 |
| S4b | | |
| S5 | PASS | 5.2 빌더 저장 → accepted+proposed, group_seq 1. 5.5 선점: B가 SQL로 seq 2를 먼저 넣자 A 저장이 `다른 참가자가 먼저 게임을 등록했습니다…`로 막히고 팝업이 남아 목록이 「2건 · 새 게임은 3번부터」로 갱신, 재저장 성공(5.6). [자동 대진표]·[게임 입력] 공존(5.7). 5.8 종료 후 세션 0·정산 true, B 뱃지 0·[게임 추가]만(5.9). 좌석 확인은 SQL |
| S6 | PASS | 강퇴(confirm 4줄) → 명단 소멸·`RoomRemovedNotice`·내 목록 제외·리스트 칩 `강퇴됨`. B의 초대 검색에는 없음, A의 검색에만 후보 → 즉시 초대 → C 수락 복귀. [내보내기]는 게임 없는 행에만(B·본인 없음). 6.11 나가기 → `/match-rooms`, 6.12 K-1 confirmed + **F-13 발견(뱃지 1 잔존)**, 6.13 비밀번호 재입장으로 복귀 |
| S7 | PASS | 카드 칩 `비밀번호 입장` → 게이트(제목·방장·참가 수) → `0000` 실패 문구 → `1234` 입장(URL 불변, joined + 풀 append 확인) → 방장 비밀번호 변경(`ab` 거부·`abcd` 성공) → B는 옛 비번 실패·새 비번 성공 → 비참가자 D는 게이트, 명단 비노출 |
| S8 | PASS(기대 수정 1) | B 기준: 뱃지 2 = 카드 2(S6 실제 차례 + S7 세션 차례 F-11). `?tab=mine`·`/me/match-requests`·`/new?room=` 리다이렉트 OK. S6 경기일을 어제로 바꾸자 참여 중인 매칭 `진행 중`에 남고 전체 목록은 `종료된 경기`(두 축). 8.10 기대 수정 — 내 차례 우선 정렬은 참여 중인 매칭에만. 8.1~8.4는 S1·S2 중 확인 |
| S9 | PASS(기대 수정 2) | B 기준. 직접 기록 폼에는 스코어 칸이 없어(저장 뒤 카드 [결과 입력]) 9.2·9.5·9.6 기대를 흐름에 맞게 수정. 비회원 단식 → `결과 입력 대기` → 6:2 즉시 확정. 회원 선택 시 `MemberNeedsRoomNotice` + 저장 비활성(9.3), 서버 축은 코드 확인(9.4). 로테이션 직접 기록(풀 3) → 빌더 1게임 → 즉시 확정 묶음 카드(9.7). 수정(상대명 변경)·삭제(confirm) OK(9.8). 방 게임은 대기 섹션에 오지 않음(9.9) |
| S10 | PASS(기대 수정 2) | RPC 축 전부 예상 코드: `room_already_closed`(10.2·10.3) · `cannot_request_self` · `opponent_not_in_room` · `lineup_locked` · `invalid_games`(게스트끼리) · `duplicate_players` · joined 재초대 무해 · `not_room_member`. 10.1 정산 방의 B에게 [게임 추가] 잔존(F-5). 10.10 비밀번호는 `maxLength=20`이 잘라 21자 불가(기대 수정). 10.11 상대 비우면 [게임 저장] 비활성 — **모집 중 게임은 폼에서 만들 수 없다**(기대 수정, 옛 행 전용). 10.12 내리기 → `/match-rooms`, 상세 404, 기록 잔존. 10.14 `/login?next=` OK. 발견 F-14 |

### 마감
- 결과: S0~S10 전부 PASS(기대값 수정 7건 — 절차서 반영). S4b(혼합 복식)는 **미실행**(성별 배치는 `lineup-core` 유닛 테스트가 덮고 있어 후순위로 남김).
- 결함: P1 0 · P2 5(F-5·F-9·F-10·F-11·F-13) · P3 9(F-1~4·6~8·14 + F-pre-1·2) · 기지 confirmed 4(K-1·K-6·K-12 + F-13 연계). 수정 계획은 `findings.md` 「수정 계획 요약」.
- 정리 SQL: 방 0 · 기록 0 · 요청 0 · 세션 0 · A~D 멤버십 0 · A~D 개인 기록 0(테스트 계정의 이전 기록도 없었음).
- 도구 메모: Playwright MCP에서 **마우스·키보드 입력이 페이지에 닿지 않는 상태**가 간헐적으로 생겼다(`fill`은 되고 `click`·`type`은 이벤트가 문서에 도달하지 않음). 같은 컨텍스트에서 `context.newPage()`로 새 탭을 열면 즉시 복구된다 — 절차서 실행 시 각 단계 앞에 `pointerdown` 도달 검사를 두고 실패하면 새 탭으로 갈아탄다(README 실행 방법 참조). 네이티브 `confirm()`은 `page.once('dialog', d => d.accept())`로 처리했다.

### 단계별 메모
(FAIL·SKIP한 단계만 `#번호 — 무엇이 달랐는지 — findings ID`)
- S4b — SKIP(시간·우선순위). 다음 실행에서 E·F 계정으로 수행.

## 2026-09-11 · Week 50 두 번째 실행 — S4b·S11 + 결함 수정 뒤 재실행

- 커밋: `f1368b8` 기준(수정 전) → 각 묶음 커밋 뒤 재실행
- 실행: Claude(Playwright MCP) + Supabase MCP SQL 보조
- 계정: A 남자01 · B 남자02 · C 남자03 · D 남자04 · E 여자01 · F 여자02

| 시나리오 | 결과 | 비고 |
|---|---|---|
| S4b | PASS | 혼복 각 팀 남1여1(`남자02 · 여자01 vs 남자01 · 여자02`), 여자01 제외 시 `혼합 복식 대진에는 참가자가 4명 이상 필요합니다.`(게임 0), 복원 후 저장 → A 제안 → B·E·F 확인(SQL) → 세션 때문에 미정산(F-11 재관찰) → 종료 후 `종료`. F-10 재관찰(`결과를 입력한 사람: 나 · 남자01`) |
| S11 | PASS | 단식 4명·2면: 힌트·초기값 `4경기 · 권장`(8경기·4라운드), 미리보기 라운드 묶음 `1라운드 · 10:00` + `1번/2번 코트`, 충돌 없음. 손 편집으로 충돌을 만들면 라운드 헤더 경고(저장은 막지 않음 — soft). 저장 뒤 방 게임 목록이 라운드로 묶여 그려짐(Week 44 회귀 OK). [대진 편집]에서 충돌을 만들어 저장하자 방 목록 헤더가 `[대진 편집]에서 고쳐주세요`로 경고(0075 self-check OK). A 제안 → 나머지 SQL → 정산 |
| 재실행 ① (F-5·F-11·F-13) | PASS | 커밋 `f4d1d3b` + 0077. **F-5** 정산된 방 두 곳(E2E-S11 단식·E2E-S4B 혼복)의 방장 화면에 남은 버튼은 [비밀번호 변경]·[매칭 리스트에서 내리기]·[결과 정정]뿐 — [게임 입력]·[게임 추가]·[자동 대진표]·[회원 초대]·[비회원 초대] 전부 사라짐. **F-11** 게임 1/1 확정 + 세션 미확정인 E2E-S50에서 방장 A는 배너 `모든 결과가 확정됐습니다 — 게임 입력을 종료하면 매칭이 마무리됩니다` · 카드 필 `입력 종료` · 뱃지 1, 풀 회원 B는 필 없음 · 뱃지 0(종전에는 전원 `결과 입력` + 뱃지 1). **F-13** 배정된 경기가 있는 B는 [방 나가기] 대신 `배정된 경기가 있어 나갈 수 없습니다…` caption, 경기 없는 E는 나가기 성공(→ `/match-rooms`, 방은 참가 4명·「비밀번호 입장」) |
| 재실행 ② (F-9·F-10) | PASS | 커밋 `56f689a`. E2E-S50에서 **파트너 D**가 제안한 뒤 상대 B가 [결과 확인]을 열면 `남자04님이 제안한 결과 (내 관점)` — 종전에는 상대팀 이름(`남자01 · 남자04`)이 그 자리에 왔다. **F-10** 행의 좌석 명단은 `확인 대기: 나 · 남자01 · 남자03` — 뷰어 본인(남자02)이 실명으로 다시 세어지지 않는다 |
| 재실행 ③ (문구) | PASS | 커밋 `38fcf3c`. **F-1** eyebrow가 `남복 · 하드`·`단식 · 하드`·`혼복 · 하드`(종전 `로테이션 복식`·`자유 기록`). **F-2·F-3** 초대 카드 `9월 11일 10:00~12:00 · E2E-S51 · 남복` + `수락하면 방 참가자로 등록됩니다. 방 안 게임은 상대 확인을 거쳐 양쪽 기록에 남습니다.` **F-4** 룸 행 배지는 `결과 확인 대기` 하나. **F-6·F-7** 정정 팝업 `정정 사유 (선택, 0/200)` · `…매칭 룸에서 다시 입력합니다`. **F-8** 대진 카드 `여자01 · E2E게스트 → 1.5 · 비회원 1`(게스트 대체값 3.9가 사라짐). **F-14** 없는 방 URL → `내려간 매칭입니다` + [매칭 리스트로 돌아가기]. **F-pre-1** `?notice=invite_failed` → 룸 상단 안내 |
| 0077 롤백 스모크 | PASS | ① 정산 방 `create_room_game` → `room_already_closed` ② 정산 방 direct insert → RLS 거부 ③ 경기 있는 회원 `leave_match_room` → `leave_member_has_games` ④ 경기 없는 회원 → `declined` ⑤ 회귀: 미정산 방 `create_room_lineup` → OK ⑥ 회귀: 경기 있는 회원 `kick_room_member` → `member_has_games` |
| 정리 | PASS | 정리 SQL 후 rooms·matches·requests·sessions·고아 세션 전부 0 |

재실행 픽스처는 `E2E-S50`(복식·방장 A·참가 B/C/D/E·대진 1게임 A+D vs B+C)과 `E2E-S51`(E에게 초대만 걸어 둔 방)을 SQL/RPC로 세웠다 — 대진 저장은 `create_room_lineup`을 쓴다(미확정 로테이션 방에서 `create_room_game`은 `room_not_ready`로 막힌다. 복식 방의 상호 확인 게임은 대진표 경로로만 생긴다).

| 재실행 ④ (K-6) | PASS | 커밋 `a93d190` + 0078. `E2E-S52`(복식·4명·1면·10:00~12:00)에서 자동 대진표를 30분으로 열고 게임 하나를 지워 **3라운드**로 만들어 저장 — 팝업 미리보기 `10:00·10:30·11:00`, 저장 뒤 방 목록도 `10:00·10:30·11:00`. 종전에는 방이 120÷3=40분으로 역산해 `10:00·10:40·11:20`으로 갈렸다. DB `match_rooms.slot_minutes=30` 확인. 0078 롤백 스모크 8건 통과(슬롯 없이 저장 → null 유지 / 30 저장 / 45 재저장은 마지막이 이김 / 상세 RPC slotMinutes / 범위 밖 `invalid_slot_minutes` / 편집(replace)은 슬롯 보존 / 정산 방 `room_already_closed` / 참가자 `not_room_host`), 오버로드 1개 확인 |

| 0083 롤백 스모크 | PASS | 0083 + 0083b(cleanup 트리거). 실 데이터 정산 방에서 ① 방장 `close_match_room` → `closed_at` 세팅 ② 두 번 닫기 → `room_closed` ③ requester `reopen_match_result` → `room_closed` ④ `invite_room_members` → `room_already_closed` ⑤ 타인 `reopen_match_room` → `not_room_host` ⑥ 방장 `reopen_match_room` 뒤 정정 통과 → `is_settled=false`. 합성 픽스처(방장 자유 기록 1건 방)에서 ⑦ 닫힌 방 direct 행 DELETE → 트리거 `room_closed` ⑧ 스코어 비우기 → recompute `room_closed` ⑨ 다시 연 뒤 DELETE 통과 + 마지막 행이라 방 삭제. **S12 브라우저 실행은 미실시** |

**도구 메모** — Playwright MCP의 입력 미도달이 이번에도 나왔다(첫 탭에서 `click`이 팝업을 열지 못함). `page.context().newPage()`로 같은 세션의 새 탭을 여는 우회가 매번 통했다. 네이티브 `confirm()`은 `page.on('dialog', d => d.accept())`를 **클릭 전에** 걸어야 하고, `once`는 첫 시도가 실패하면 소진되어 다음 클릭에서 창이 그대로 막는다.

| Week 54 문구 정리 | 자동 검증만 | `npx tsc --noEmit` · `npm run lint` · `npx vitest run`(68파일 882건) · `npm run build` 통과. DB 변경 없음. **브라우저 스모크는 미실시** — 절차서의 기대 문구를 코드와 함께 고쳤으므로(방장→호스트, [방 나가기]→[매칭 나가기], [방 닫기]→[매칭 닫기], 칩 `강퇴됨`→`내보내짐`, 배지 `WIN`/`LOSS`→`승`/`패`, [수락]→[참가 수락], [비회원 초대]→[비회원 등록], 라인업 경고의 '자유 기록'→'개인 기록으로만') **S1 1.5/1.12 · S6 6.5 · S12 12.1/12.4**를 다시 돌려 화면과 절차서가 맞는지 확인해야 한다. 함께 볼 것: 두 목록 화면의 [+ 매칭 만들기]가 탭 바 **위**에 테두리 칩으로 있는지, 프로필에서 AI 코칭 카드와 클럽 유도(빈 상태·온보딩 3단계)가 사라졌는지, 데스크톱 헤더에 [클럽 찾기]가 없는지 |

| Week 60 아이디 로그인·비밀번호 규칙 | PASS | 0085 롤백 스모크 12건(소문자 정규화·phone nullif 유지·23505·23514·taken/resolve·탈퇴 제외·재사용·null 가입·anon 권한) → 적용. 브라우저(localhost, Playwright MCP): 없는 아이디 → 비밀번호 오류와 같은 문구 / `NAMJA01`+`123123` 아이디 로그인(소문자 정규화) / `bbb@bbb.com` 이메일 로그인 / 프로필 설정에서 `namja01` 실시간 「이미 사용 중」 → `Namja02` 저장 → 읽기 전용 `namja02` / 그 아이디로 로그인 / 가입 폼 아이디 중복 문구·비밀번호 체크리스트 3/4·제출 잠금 / 비밀번호 변경에 `abcdefgh` → 서버 정책 문구 / `?notice=weak_password` 배너. 도구 메모: [저장하기] click이 한 번 닿지 않아 `form.requestSubmit()`로 우회. 남은 것: 대시보드 최소 길이 8, 프로덕션 재설정 메일 도달 확인(CLAUDE.md Week 60 잔여) |

| Week 61 아이디 찾기·비밀번호 찾기 | PASS | 0086 롤백 스모크 8건(일치 마스킹·정규화·불일치 null·email_only·social google·탈퇴 전후·anon 권한) → 적용. 브라우저(localhost): `/find-id` 남자01+`AAA@AAA.COM`(공백 포함) → `na*****` / 남자03+`ccc@ccc.com` → 이메일 로그인 안내 / 구글 계정 → 구글 카드 + Google 버튼 / 남자01+`bbb@bbb.com` → 불일치 문구. `/auth/confirm?token_hash=bogus&type=recovery&next=//evil.example` → `/forgot-password?error=expired` 배너. `/forgot-password`에 `namja01` → success 화면, auth_logs에 `/recover 200` + `mail.send mail_to=aaa@aaa.com mail_from=noreply@mail.app.supabase.io`(기본 SMTP — 커스텀 미설정 확정). 폼 제출은 `requestSubmit()`로(Playwright MCP click 미도달 우회). |

## 2026-09-15 · Week 62 전 구간 A-Z 실행 (Phase별 블록)

- 커밋: `73e8528` 기준(문서만 변경, 코드·DB 변경 없음)
- 실행: Claude(Playwright MCP) + Supabase MCP SQL 보조 · 구글은 사용자 수동 조작(M)
- 환경: 원격 Supabase, `npm run dev`, 뷰포트 1280×900(J3는 390×844). auth settings: `external.google=true`, `mailer_autoconfirm=true`, 비밀번호 최소 길이는 settings에 노출되지 않아 A2.7에서 실측. `PASSWORD_RESET_MAIL_ENABLED=false`.
- 계정: A~F(README) + G 구글(M) + 신규 `e2e<n>@e2e.test`
- 절차서: `account-scenarios.md`(A0~A9) · `match-room-scenarios.md`(S0~S15) · `record-scenarios.md`(R1~R5) · `journey.md`(J1~J3)

### Phase 0 — 준비·현행화·선등록

| 항목 | 결과 | 비고 |
|---|---|---|
| 정리 SQL·회원 정리 | PASS | rooms 0 · matches 0 · requests 0 · sessions 0 · e2e 회원 0. A~F 전원 joined 0·개인 기록 0·클럽 0 |
| 계정 현황 | — | A `namja01` · B `namja02` · C~F login_id null · G 구글(login_id null, ntrp 3.0) |
| 절차서 현행화 | 완료 | S9.3·9.4 → 0082 동작(회원 선택 = 비노출 방), S4 4.14/4.15 순서, 축 커버리지에 S11~S15, 회귀 고정 행에 0082·Week 54 |
| 신규 절차서 | 완료 | account(A0~A9 · 76단계) · record(R1~R5 · 42단계) · journey(J1~J3 + 체크리스트 7) · S13(13)·S14(16)·S15(10) 추가 |
| 선등록 | 완료 | findings F-pre-4~9 · improvements U-pre-1~4 |

### Phase 1 — 계정 생명주기 A0~A8

| 시나리오 | 결과 | 비고 |
|---|---|---|
| A0 | PASS | 보호 5경로 → `/login?next=`(쿼리 인코딩 보존) · 공개 5경로 열림 · `next=//evil` 거부 → 프로필 · 로그인 상태 `/login`·`/signup`·`/` → 프로필 · `/find-id`·`/forgot-password`는 로그인 상태에서도 열림(U-4) · 로그아웃 뒤 보호 경로 재차단 |
| A1 | PASS(FAIL 2) | 빈 폼 [회원가입] 비활성 · 아이디 4종 문구(`abc`·`한글`·`namja01` 중복·`e2esignup1` 가능) · **`ABC_1`은 대문자 그대로 「사용 가능」**(U-1) · 이메일 중복 + [로그인]·[비밀번호 찾기] · 체크리스트 2/4→3/4→4/4 · 확인 불일치 · 이름 숫자 금지·20자 잘림 · 닉네임 2자·`남자닉네임01` 중복(절차서의 `남자01`은 이름이라 수정) · 휴대폰 `010-1`·`399-…` 거부, `010-`만 → 미입력 · 셋 미선택 시작 + 안내 · 시작일 미래 거부 · 라켓 기타 required · 동의 미체크 브라우저 차단 · 확인 불일치 시 잠금. **1.12 서버 거절(닉네임 충돌) 뒤 라켓 기타 브랜드명·동의 체크가 초기화**(F-17) + **클라 「사용 가능한 닉네임입니다」와 서버 「이미 사용 중」이 동시에 표시**(F-18). 1.13 착지 `/clubs` 더미 목록(**F-pre-4 confirmed → P2**). 1.14 SQL 전부 일치(login_id 소문자·phone·기본 아바타·auth 1행·메타). 1.15 anon RPC 3종 정규화 true, `resolve_login_email` 열거 확인. 1.16 재가입 시 실시간 차단 |
| A2 | PASS(SKIP 1) | `namja01`/`NAMJA01 `/이메일/대문자 이메일 전부 로그인 · 실패 4종 모두 `아이디 또는 비밀번호가 올바르지 않습니다.` · `?next=/match-rooms?tab=past` 복귀 · `?error=oauth`/`deleted` 배너 · 눈 아이콘 토글(첫 탭 클릭 미도달 → 새 탭에서 PASS) · 「로그인 상태 유지」 기본 체크·동작 없음(U-pre-2 confirmed). **2.7 SKIP** — `123123` 로그인이 notice 없이 프로필 착지 → 대시보드 최소 길이 8 미설정 |
| A3 | PASS | `na*****` / email_only / social+Google 버튼 / 불일치 문구 · 공백·대문자 정규화 · 이름 숫자 허용 · anon `find_login_id` jsonb에 masked만. 3.5b·c(빈 이름·이메일 형식)는 브라우저 required/type=email이 제출 전 차단 |
| A4 | PASS | 폼 없음 + 운영자 문의 카드(mailto subject `[BASELINE] 비밀번호 초기화 요청`) · `/reset-password` → `/forgot-password` · bogus token + `//evil` → `?error=expired` 배너 · 4.2·4.5 코드 확인 |
| A5 | PASS | SQL ntrp null → 로그인·`/match-rooms`·`/profile/settings` 전부 `/onboarding/profile`(헤더·사이드바 0) · 닉네임 중복 잠금·본인 닉네임 통과 · 완료 → `next`(`/match-rooms`) · 재진입 → 프로필 · 설정 화면 넷 읽기 전용. **완성 화면이 기존 휴대폰·라켓을 프리필하지 않아 저장 시 라켓이 null로 덮였다**(U-2 — 소셜 가입자는 원래 빈 값이라 실사용 영향 없음) |
| A6 | PASS(FAIL 1) | 읽기 전용/편집 필드 구분 OK · 닉네임 중복 잠금·변경·원복(헤더는 이름을 보여 변경이 안 보임 — U-5, 성공 메시지 없음 — U-pre-3 confirmed) · 휴대폰 형식 오류(저장 버튼은 활성 — U-3)·비움 → null · 라켓·통계 비공개·기본 아바타 저장 SQL 일치 · 작은 PNG 업로드 → 스토리지 URL. **1.5MB·6MB PNG → `(main)/error.tsx` 「화면을 불러오지 못했습니다 / Body exceeded 1 MB limit…」(F-15 P1)**. 같은 파일로 `/signup` → **global-error 「예기치 못한 오류가 발생했습니다」, 입력 전부 소실**(계정은 안 생김). 6.7 C 아이디 1회 설정 `Namja03` → `namja03` 읽기 전용 → 로그인 OK. 6.8 C 컨텍스트로 `login_id·ntrp·name` 직접 UPDATE **통과**(롤백 — K-13 confirmed, DB 거울 없음). 6.9 D 시작일 1회 입력 → 잠김. 6.10은 M |
| A7 | PASS(FAIL 1) | 현재 비번 오류·정책·불일치 문구 · 변경 성공 + 폼 reset · 옛 비번 실패·새 비번 성공 · **같은 비밀번호 → 「오류가 발생했습니다…」 폴백**(F-16 — 매핑 `same as the old password`가 실제 메시지와 다름). 7.4는 M. A 비밀번호는 SQL `crypt`로 `123123` 원복 |
| A8 | PASS(M) | 사용자 조작 + auth_logs 검증. 8.1 재로그인 정상(07:28:45·07:28:58·07:29:08 세 번 모두 `/authorize` referer `http://localhost:3000/auth/callback` 전체 경로 → `/callback 302` → `/token pkce 200`). 8.2 로그아웃 상태에서 `/login?next=%2Fmatch-rooms` → 구글 → **`/match-rooms` 착지**(소셜 버튼 next 전달 OK). 8.3 구글 사진 표시·8.4 `/profile/settings` 크래시 없음(사용자 확인). **8.5 재가입**: 두 행(auth·public) 삭제 → 사용자가 구글 로그인 → `/onboarding/profile` → 전부 입력 → [시작하기]. SQL: 07:33:42 트리거 생성(이름 `J Pi`·닉네임 `pangnima` 폴백·구글 `picture`) → 07:34:41 저장(NTRP 2.0·남성·오른손·2024/04·휴대폰, **구글 사진 URL 그대로 유지**) — 데이터는 PASS. 그러나 사용자는 「회원가입 안됨」으로 인지(완료 신호 없음, 버튼 「시작하기」 — U-6). 이어서 **뒤로 가기 → [Google로 계속하기] → Runtime Error `An unexpected response was received from the server.`**(F-19 — Playwright로 재현: 세션이 있는 채 남은 `/login` 페이지에서 서버 액션 POST → 미들웨어 307 → 액션 응답 아님. 이메일 로그인 폼도 동일). 8.6 병합 SKIP(대상 계정 없음), 8.7은 A9.10 |

- 계정 상태: e2e1 가입 완료(아이디 `e2esignup1`, 온보딩 완료, 라켓 null). C `login_id=namja03`, D 시작일 `2019-12-01`, A 아바타·휴대폰·라켓·통계 원복.
- 도구 메모: 첫 탭에서 click 미도달 2회(눈 아이콘·체크박스) → 새 탭으로 복구. 모든 제출은 `requestSubmit()`.

### Phase 2 — 매칭 룸 S0~S15

| 시나리오 | 결과 | 비고 |
|---|---|---|
| S0 | PASS | 비로그인 `/guide` 헤더·[로그인]·6섹션·예시 6(inert, 버튼 4 비활성)·스테퍼 앵커 4·환영 팝업 0. `/match-rooms` 「이 화면 사용법」 펼침(참가 방 0) + `/guide#match-rooms` 링크. 뱃지 0 |
| S1 | PASS(기대 수정 3) | 폼 기본값·비밀번호 문구 2종·요약 `10:00~12:00 (2시간) · 코트 1면 · 참가 예정 2명이면 1인당 4경기 권장`·초대 칩 → 상세 eyebrow `단식 · 하드`·`모집 중`·명단 호스트/초대 대기(SQL 일치, secrets 1). B 뱃지 1·초대 카드·배너 [참가 수락] → `참가`·뱃지 0. A [게임 추가] → 행 `나 / vs 남자02`·`결과 미입력`·`진행 중`·배너. B 제안 6:3 → 배지 `결과 확인 대기` 하나·`결과를 입력한 사람: 나`·`확인 대기: 남자01`·`결과 확인 중`. A 반전 3:6·[결과 확인] → `종료`·남는 버튼 [비밀번호 변경]·[매칭 닫기]·[매칭 리스트에서 내리기]·[결과 정정]. 개인 카드 `상호 확인`·[결과 정정]만. `마무리됨`·`결과 확정`·날짜 축 `종료된 경기`. **기대 수정**: 시각 선택지 라벨 `10시`(`10:00` 아님) / 초대 카드 제목 `나를 참가자로 입력한 경기` / 단식은 `n/2명 확인` 진행 배지 없음(`formatConfirmProgress` total≤2). 관찰: 초대 대기 B에게 [거절]과 [매칭 나가기]가 함께(U-7) |
| S2 | PASS | 픽스처 SQL(seed→`create_match_room`→초대→수락). 제안 6:4 → [제안 수정] 6:2(프리필·`confirmed_by=[A]` 유지) → B [결과 확인] 팝업 → [이의 제기] 모드(사유 `0/200` 카운터, 201자 → 200 잘림·`200/200`) → `disputed`·`내가 이의 제기`·`내 이의 사유: 점수 오기` → A `남자02님 이의`·[다시 입력]·배너 `결과를 다시 입력해주세요`·필 `다시 입력`·뱃지 1 → 재제안 → `남자02님 이의 후 재입력`·`직전 이의 사유` → B 배너 `다시 입력된 결과를 확인해주세요`·필 `결과 확인` → 확정 → [결과 정정] 팝업(`정정 사유 (선택, 7/200)`, `…매칭 룸에서 다시 입력합니다`) → `결과 확인 중`·`내가 이의 제기`·프리필 6:4 → 재제안 `(2차)` 배지 → B 확인 → 정산. 2.14 양쪽 `상호 확인`. 2.13은 S14로. **관찰**: disputed 상태에서 상태 배지 `이의 제기` + 액션 배지 `내가 이의 제기`/`남자02님 이의` 둘(F-20 — F-4 잔여). 정산 뒤에도 `배정된 경기가 있어 나갈 수 없습니다. 결과를 마무리하거나…`(U-8) |
| S3 | PASS | 힌트 없음(호스트 1명) → [비회원 등록] 팝업(손·성별·NTRP 선택 + `몰라도 그냥 두세요`) → 게스트1 등록·동명 거부·회원 이름 거부·게스트 2~4 → 힌트 `참가 예정 5명이면 1인당 1경기 권장`(헤더는 `참가 1명` — 비회원 미포함, U-9). [게임 추가] 후보에 게스트 없음(K-3 confirmed — 전역 회원 `테스트가입자`는 뜬다). [자동 대진표] `1경기 · 권장`·경고 2종·팀 전력 `3.9`/`비회원 1`·`쉼` 명단·출전 횟수 → 2경기 → spot 블록 + [권장값으로 맞추기](`총 5경기`) → 복귀 → 게임 2 [수정] 상대 교체(`대진을 직접 고쳤습니다…`) → 게임 3 [삭제]·[게임 추가](`게임 3: 아직 비어 있는 자리가 있습니다.` + 저장 잠금) → 채움 → [다시 뽑기](편집 폐기) → [3경기 저장]. SQL: direct 3행 `origin='lineup'`, `created_at` 상이(0075), `slot_minutes=30`, 요청 0. [대진 편집] 자리 교체 저장 → 즉시 확정 6:0(`결과 저장`) → 편집 목록 2건 → [빼기] confirm 4줄 수락 → 명단에서 파생 `비회원` 행으로 남음(Week 49 동일) → 나머지 확정 → `종료`. 개인 카드 [수정]·[삭제] |
| S4 | PASS(FAIL 1, 기대 수정 1) | 픽스처 SQL(rotation seed·2면·초대 3·수락). 힌트 `1면 기준 1인당 4경기 권장`·빈 상태 호스트 문구·D [게임 입력]만. [자동 대진표] 초기 `4경기 · 권장` → 2경기 → 경고 `참가자 4명으로는 한 번에 1면만 돌릴 수 있습니다.` **기대 수정**: 실효 1면이라 라운드 헤더 없음(`게임 1· 10:00`·`게임 2· 10:30`). 저장 → 요청 2행. A 제안 6:4 → `1/4명 확인`·`확인 대기: 남자04 · 남자02 · 남자03` → B 팝업 `남자01님이 제안한 결과 (내 관점)`·`(지금까지 1/4명 확인)`(F-9) → B 확인 → `확인 완료`·`2/4명 확인`·[이의 제기] 유지 → C SQL 3/4 미정산 → D `확인 완료: 남자02 · 남자03 / 확인 대기: 나` → 확정 `진행 중`. 4.12 [내보내기] 0개 + SQL `member_has_games`. 4.13 게임 2 SQL 확정 → 상세 배너 `모든 결과가 확정됐습니다 — 게임 입력을 종료하면…`(호스트) **그러나 참여 중인 매칭 카드에 `게임 입력 종료` 필 없음 · 뱃지 0**(**F-21 P2** — `fetchRoomGameTallies`가 RLS 아래에서 호스트가 requester가 아닌 대표 행을 못 읽어 total=0. 이번 대진은 requester가 남자04·남자02). B 뱃지 0 ✓. 4.14 개인 카드 관점 승/패 OK. 4.15 [게임 입력 종료] confirm → `종료`·[결과 정정]×2 |
| S4b | PASS | 픽스처 SQL(혼복·1면·B·E·F). [자동 대진표] 1경기 → 각 팀 남1여1(`남자02 · 여자01 vs 남자01 · 여자02`, 성별 경고 없음) → 여자01 제외 → `참가자 3명`·`혼합 복식 대진에는 참가자가 4명 이상 필요합니다.`·게임 0 → 복원 → 저장 → A 제안 6:3 `1/4명 확인`·`확인 대기: 남자02 · 여자02 · 여자01` → B·E·F SQL 확인 4/4 → 세션 남아 미정산 → [게임 입력 종료] → `종료` |
| S11 | PASS(기대 수정 1) | 픽스처 SQL(단식·2면·B·C·D). 힌트 `코트 2면 … 1인당 4경기 권장` → [자동 대진표] 초기 `4경기 · 권장`·총 8경기·라운드 4(`1라운드 · 10:00`~`4라운드 · 11:30`)·`1번/2번 코트`·충돌 없음 → 2경기 → 2라운드 + spot 추천 → 게임 1 [수정]으로 게임 2의 사람을 넣자 `남자03 — 같은 라운드에 두 번 배정되어 있습니다. 동시에 두 코트에 설 수 없습니다.`(저장은 막지 않음) → [다시 뽑기] → [4경기 저장] → 방 목록 라운드 묶음 `1라운드 · 10:00`·`2라운드 · 10:30`(**기대 수정**: 30분 슬롯이라 11:00이 아니라 10:30) + `N번 코트`. [대진 편집]에서 충돌을 만들어 저장 → 방 헤더 `… [대진 편집]에서 고쳐주세요.`(0075 self-check) → 고쳐 저장 → 경고 소멸·라운드 유지. A 제안 → 나머지 SQL → `is_settled` 즉시 true |
| S5 | PASS | 픽스처 SQL(남복·1면·B·C·D). B [게임 입력] → 빌더 `참가자 3명: 남자03, 남자04, 남자01` → [게임 추가] 파트너 C·상대 A·D·6:3 → [게임 1개 저장] → 행 `결과 확인 대기`·`1/4명 확인`(finalize = accepted + 제안). A [결과 확인] → `확인 완료`·`2/4명 확인`·세션 남아 `진행 중`. **선점**: A가 빌더에서 게임 2를 준비한 사이 B가 SQL `finalize_rotation_session(…, 2)` → A 저장 시 `다른 참가자가 먼저 게임을 등록했습니다. 목록을 확인한 뒤 다시 저장해주세요.` + 팝업 유지 + `이 일정에 등록된 게임 2건 / 새 게임은 3번부터` → 재저장 성공(게임 3). [자동 대진표]·[게임 입력] 공존(K-12). [게임 입력 종료](confirm) → [게임 입력] 소멸·[게임 추가] 등장(호스트·B 모두) → 남은 제안 SQL 확인 → 정산. SQL 형식 메모: finalize의 games는 `{partner,opp1,opp2:{userId,name}, sets:[{me,opp}]}` |
| S6 | PASS | 픽스처 SQL(단식·B·C 참가·A–B 게임). [내보내기] 1개(B·본인 행 없음) → confirm 3줄 → 명단에서 C 소멸 → C 상세 `호스트가 이 매칭에서 회원님을 내보냈습니다.`(비밀번호 입력창 없음) → C 참여 목록 없음·뱃지 0 → 전체 목록 칩 `내보내짐`. B 검색 후보 없음 / A 검색 후보 → 즉시 초대 `초대 대기` → C 뱃지 1 → [참가 수락] `참가`. B 하단 [매칭 나가기] 없음 + caption. SQL `leave_member_has_games`·`host_cannot_leave`. C [매칭 나가기] confirm → `/match-rooms`·카드 `비밀번호 입장`·상세 게이트 |
| S7 | PASS | 픽스처 SQL(남복·초대 없음). C 카드 `비밀번호 입장` → 게이트(`호스트 남자01 · 참가 1명`) → `0000` 거부 → `1234` 입장(URL 불변·`참가`·풀 append) → A [비밀번호 변경] `ab` 거부·`abcd` `비밀번호를 변경했습니다.`(팝업은 남는다) → B `1234` 거부·`abcd` 입장 → D 게이트·명단 비노출 |
| S8 | PASS | 전이별 스냅샷은 S1·S2·S14에서 채집(초대 1 / 제안 직후 확인 좌석 1 / 이의 직후 제안자 1 / 정산 0 / 방관자 0). `?tab=mine`·`/me/match-requests` → `/me/match-rooms`, `/new?room=` → 상세. 8.9 S6 경기일 어제 → 참여 중인 매칭 `진행 중`(필 `결과 입력`·뱃지 1) / 전체 목록 `종료된 경기`(두 축). 8.5 뱃지는 방 수(A: S14D 게임 3건 미확정 + S6 = 2). 8.10 정렬은 미측정 |
| S9 | PASS | [+ 직접 기록] → 폼(스코어 칸 없음). **9.3(0082)**: `남자02` 선택 → `회원 연결됨`·안내·`경기 시간 *`/`코트 면 수 *` 등장·버튼 [매칭 만들고 초대] → 비회원으로 되돌려 저장 → `결과 입력 대기 | 1건` 카드([결과 입력]·[수정]·[삭제]) → 6:2 즉시 확정 `승`. 9.4 수정 화면의 자동완성은 회원을 **후보에 내지 않는다**(options 0) — 서버 `DIRECT_RECORD_MEMBER_ERROR`는 코드. 9.7 로테이션 비회원 4 → 대기 세션 카드 → 빌더 1게임 → 즉시 확정 묶음(`로테이션 · E2E-S9 | 1게임 · 1승 0패`). 9.8 [수정] 상대명 변경·[삭제] confirm OK. 9.9 S6 방 게임은 대기 섹션에 없음 |
| S10 | PASS | SQL: `room_already_closed`(lineup·guest on settled)·`cannot_request_self`·joined 재초대 무해·`opponent_not_in_room`·`lineup_locked`(미정산 방에 결과 있는 라인업 게임 교체)·`invalid_games`(게스트끼리)·`duplicate_players`·`not_room_member`(비참가 leave). 브라우저: 정산 S1에서 B 버튼 [결과 정정]뿐, 상대 자동완성에 본인 없음, 상대 비움 → [게임 저장] 비활성, 비밀번호 `maxlength=20`, [매칭 리스트에서 내리기] → `/match-rooms`·`내려간 매칭입니다` + [매칭 리스트로 돌아가기], 비로그인 → `/login?next=` |
| S12 | PASS(첫 브라우저) | A 정산 S1: 칩 `종료`·안내 `… [매칭 닫기]로 마감할 수 있습니다.`·[매칭 닫기]. 미정산 S6에는 없음 + SQL `room_not_settled`. [매칭 닫기] confirm → 칩 `마감`·[다시 열기]·[결과 정정] 소멸·안내. B: 안내 `호스트에게 다시 열기를 요청하세요`·버튼 0 + SQL `reopen_match_result` → `room_closed`. 카드 칩 `마감`(두 목록)·개인 카드 배지 `마감`([결과 정정] 없음). 12.8 호스트 자유 기록 `/edit` → 방으로 리다이렉트, 소유자 DELETE·UPDATE 정책 0행. 12.9 `room_closed`(kick·enter)·`room_already_closed`(invite·replace). 12.10 닫힌 방(S12 픽스처) 초대를 C가 수락 → joined. 12.11 [다시 열기] → `종료`·[매칭 닫기] 복귀 → B [결과 정정] → `결과 확인 중`·[매칭 닫기] 소멸(제안자 B가 다시 입력할 차례, A는 대기 — 정정한 사람이 원 제안자라 일관). 12.12 `not_room_host`·`room_not_closed`·두 번 닫기 `room_closed` |
| S13 | PASS(FAIL 1) | A 직접 기록 + `남자02` → 안내 3문장·`2시간`/`1면`·[매칭 만들고 초대] → `?notice=direct_room`·`비공개 매칭을 만들고 초대를 보냈습니다.`·eyebrow `단식 · 하드 · 비공개`·[매칭 삭제]([비밀번호 변경] 없음). SQL `is_listed=false`·secrets 0·seed 삭제. 전체 목록 두 탭·탭 숫자 제외. B 뱃지 → 초대 카드 → 수락 → 카드 칩 `비공개`. C URL → `초대받은 사람만 볼 수 있는 매칭입니다.`(입력창 없음). SQL `enter_match_room`·`update_match_room_password` → `room_not_listed`. 13.9 게임·협상·정산 정상. **13.10 비노출 방에서 초대 [거절] → 호스트 재초대가 `on conflict do nothing`으로 무효(declined 유지) + 호스트 검색 후보에도 없음 → 영구 진입 불가, 화면은 「초대가 필요합니다」라 말한다(F-22 P2)**. 13.11 복식(회원 1 + 비회원 2) → 비노출 로테이션 방 + 게스트 즉시 등록 + `남자02 초대 대기` |
| S14 | PASS(기대 수정 2) | 단식 4게임(SQL 전수): 제안→확인 / 제안자 수정(cb=1 유지) / 사유 없는 이의 → 재제안 → 확인(dc=1) / 이의 2회(dc=2, 마지막 사유 보존) → 확정 / reopen(dc=3, 양쪽 `set_scores=[]`) → **B(이의자 아님)가 재제안** → A 확인 / 동시성 `result_already_proposed` / 자기 제안 이의 `cannot_dispute_own_proposal` / 정산 뒤 재확인 → **`result_not_proposed`**(기대 수정) / 방 정산. 복식(브라우저+SQL): A 제안 6:0 → B 같은 값 `승 6-0`·`확인 대기: 나 · 남자03 · 남자04` → B 확인 → `확인 완료`·`2/4` + [이의 제기] → **확인한 좌석의 이의**(팝업 `이미 확인했습니다 — 확정 전까지는 이의를 제기할 수 있습니다`) → `내가 이의 제기`·A 배너 `결과를 다시 입력해주세요`·A 뱃지 2 → 재제안 `남자02님 이의 후 재입력`·`1/4` → B·C 확인 → C 재확인 `result_already_confirmed_by_seat` → D 확인 → 확정. **14.13** D `deleted_at` 세팅 시 `request_result_seats` 3명(D 제외) — 롤백. **14.6 UI**: A가 팝업을 열어 둔 사이 B가 SQL 제안 → A [확인 요청] → 팝업이 닫히지 않고 **B의 제안 검토 모드로 갱신**(`남자02님이 제안한 결과 (내 관점)`). **14.12** 방관자 E 행: 상태 배지만(`결과 미입력`·`결과 확인 대기`) — 액션 영역 배지 없음(**기대 수정**: bystander 배지는 룸 행에서 그리지 않는다), 버튼 없음·뱃지 0. 14.14 전부 확정 → **A가 requester인 이 방에서는** 카드 필 `게임 입력 종료`·뱃지 1·배너 일치(F-21 대조 증거) → 종료. 14.15 reopen → 재제안 → 3명 확인 → 정산 복귀 |
| S15 | PASS | B(참가) 화면: 정산 S14D 버튼 [결과 정정]뿐, 미정산 S6 [회원 초대]·[비회원 등록]·[게임 추가]·[결과 입력] — 호스트 6종 없음. [+ 매칭 만들기]는 탭 바 위. SQL 6종 `not_host`/`not_room_host` ×5 + 방 삭제 RLS 0행. 초대 대기 B(S13C): [참가 수락]·[거절]·[매칭 나가기](U-7) + SQL `not_room_member` ×3. 비참가 D: `not_room_member`. 15.6 정산·미마감 S3의 direct 행 소유자 UPDATE(스코어 비움) → `is_settled=false`(롤백) — F-pre-9 = K-9 의도(정정 상당) → `known`. 회원 검색 1자 → 후보 0, `남자` → 본인·초대 대기 제외. 15.8 20명 초과는 SKIP(회원 수 부족 아님 — 시간) |

- **Phase 2 정리 SQL은 Phase 3(R3 통계 대조) 뒤로 미룬다.** 남긴 방: S1(정산, B 정정 후 미정산)·S2·S3·S4·S4B·S11·S5·S6(미정산)·S12·S13·S13B·S13C·S14S·S14D. S7은 내리기 테스트로 삭제(세션이 `room_id null`·`court_name='E2E-S7'`로 남는다 — 정리 SQL에 `court_name like 'E2E-%'` 세션 삭제를 추가).
- 도구 메모: base-ui Select는 옵션 클릭이 닿지 않아 **키보드**(focus → Enter → Home/ArrowDown×n → Enter)로 고른다. 탭이 오래되면 click 미도달이 늘어 배치마다 새 탭. `page.on('dialog')`를 같은 페이지에 두 번 걸면 `already handled` 예외.

### Phase 3 — 기록·통계 R1~R5

| 시나리오 | 결과 | 비고 |
|---|---|---|
| R3 | PASS(FAIL 1) | **통계 대조**: A의 `personal_matches`(has_result) 게임 단위 SQL 집계 = 전체 19승 3패(22) · 단식 12/1(13) · 남복 6/2(8) · 혼복 1/0(1) ↔ 화면 4카드 **전부 일치**. 헤더 `22 경기 · 주력 단식 · NTRP 4.229`(개인 레이팅 갱신 확인 — 3.4)·티어 `마스터 46/100P`·최근 폼 `W W L …`·`현재 2연승`. 표면별 하드 22경기·파트너 케미 `남자닉네임02 5연승 케미 89`·복식 코트 성향 듀스 9경기. **`NTRP 대비 성적`은 22경기에서도 `경기 데이터가 없거나…`**(F-23 — 카드가 클럽 `bundle.matches`만 본다). `상대 손잡이별 성적` 총 28경기가 전체 22경기를 넘는다(복식 상대 2명 각각 셈 — U-11). 통계 비공개 스위치 즉시 반영·원복. `get_user_match_stats_v2`는 클럽 대진표 전용이라 0 |
| R4 | PASS(FAIL 1) | 0경기 신규 계정: `ProfileEmptyGuide`·`StatsEmpty`·프리뷰 빈 상태·체크리스트 **`2단계 중 1단계 완료`로 시작**(U-pre-1 confirmed). S6 입장 뒤 체크리스트 자체가 사라짐(`모든 준비를 마쳤어요!`는 마지막 단계를 프로필 밖에서 끝내면 볼 수 없다 — 기대 수정). **타인 프로필(B→A, 공개 상태): `0 경기 · 전적 데이터가 아직 없어요 · 참여한 경기가 없습니다`** — A는 22경기 확정(F-24 P2, 타인 요약이 클럽 데이터만 본다). 비공개 상태: 4카드 `*` + `승률을 공개하지 않은 유저입니다`. 본인 비공개 화면에 `클릭해서 보기`가 숫자와 함께 보인다(누르면 공개 전환 — 미실행). 없는 userId → 404(CTA `클럽 목록으로 돌아가기` — U-pre-4 confirmed). 클럽·통합 탭 `준비 중` |
| R1 | PASS(기대 수정 1) | B 직접 기록: 시각 셀렉트(키보드)·중복 일정 경고 `이 시각에 이미 등록된 경기가 있습니다 — 남자01 / 남자01 / 로테이션 경기…`(같은 이름 반복 — U-12, 저장은 막지 않음)·코트명 `maxlength=40` 잘림(문구 없음)·NTRP 7.5는 인라인 문구 없음(서버 검증) → `결과 입력 대기` 카드 → 결과 팝업: **0-0 저장 거부**, 6게임째 `게임은 최대 5개까지 등록할 수 있습니다.`, `100`은 number 입력 제약으로 들어가지 않음 → 저장. [수정] 상대명·날짜 변경 저장 OK. 페어 고정 복식(비회원 3) → 2게임 6-4·4-6 → **multi 그룹**(`2게임 · 1승 1패` 헤더 + 가상 카드 `게임 1·2`, 액션은 헤더) → 헤더 [삭제] confirm → 소멸. 수정 화면 자동완성은 회원을 후보에 내지 않는다(9.4와 동일). 타인 행 `/edit` → 404. 마감 방·상호 확인 행 `/edit` → `/me/personal-matches`. **관찰**: 내려진 S7 방의 로테이션 세션이 B의 `결과 입력 대기`에 `로테이션 · 참가자 2명 · 2/2명 수락 · 게임 미입력`으로 남는다(F-26) |
| R2 | PASS | 월별 그룹 `2026년 9월 | 8승 11패 | 승률 42%`, 필터 단식 10·복식 9·전체 19, 배지 `승/패`·`상호 확인`·`마감`, multi·rotation 헤더 |
| R5 | PASS | 사이드바 5항목 + 뱃지, rail 점, 모바일 시트 5항목·뱃지·테마 토글·로고 없음, 테마 새로고침 유지, 세 화면 `PageGuide` → `/guide#personal-matches` 앵커가 `개인 경기 결과` h2를 상단에. **뱃지 항등식**: B 뱃지 3 = 필 카드 2 + 초대 1. 비로그인 `/guide` 로고 → `/clubs` → `/login?next=%2Fclubs`(U-pre-4). 방 상세의 활성 메뉴 판정은 클래스 휴리스틱이라 미확정(K-11 known 유지) |

### Phase 4 — 여정·횡단 J1~J3

| 시나리오 | 결과 | 비고 |
|---|---|---|
| J1-a | 관찰 | Phase 1·2에서 밟은 경로로 셈: 가입(필드 14 + 토글 4 + 체크 1 + 버튼 1) → **착지 `/clubs` 더미 목록(막힘 1 — 다음 행동 없음)** → 사이드바 `매칭 리스트`(1) → 카드(1) → 비밀번호 입력·[입장](2) → [게임 추가]·상대 선택·[게임 저장](3) → [결과 입력]·스코어·[확인 요청](3) → (상대 확인 대기 — 알림 없음) → `개인 경기 결과`(1). **화면 7 · 클릭 ≈12 · 막힘 2**(착지, 비밀번호를 어디서 아는가) |
| J1-b | PASS | e2e1 초대 흐름 실측: 로그인 착지(뱃지 1) → `참여 중인 매칭`(1) → [참가 수락](1) → 카드(1) → [게임 추가]·상대·[게임 저장](4) → [결과 입력]·스코어·[확인 요청](2) = **화면 5 · 클릭 9**, 막힘 1(호스트가 게임을 안 만든 방의 배너는 `아직 등록된 게임이 없습니다. 참가자가 모이면 게임을 추가하세요.` — "내가 추가할 수 있다"를 말하지 않음) |
| J2 | 관찰 | S4·S11·S14D 실측 합산: 매칭 만들기 ≈10클릭 → [비회원 등록] 3/명 → [자동 대진표] 2(권장값이 초기값이라 저장만) → [대진 편집] 4 → 결과 수집(게임마다 제안 3 + 확인은 상대) → [게임 입력 종료] 2 → [매칭 닫기] 2. 막힘: 상대가 확인을 안 하면 배너가 `상대의 응답을 기다리는 중입니다`뿐(재촉 수단 없음 — K-10). **모든 게임이 확정돼도 호스트가 requester가 아니면 목록 필·뱃지가 종료를 재촉하지 않는다**(F-21) |
| J3 | PASS | 390×844: `/signup`·`/match-rooms/new`·룸 상세(2면 라운드)·두 목록·개인 경기 결과·프로필·가이드 **가로 스크롤 0**, input 16px 미만 0, 자동 대진표 팝업 하단 [저장] 뷰포트 안(top 721/844). 팝업 영문 `Close` 0(자동 대진표·게임 추가·결과 입력·결과 확인·정정·비밀번호 변경·탈퇴·빌더·회원 초대·비회원 등록). 빈 상태: 두 목록 × 2탭·개인 경기 결과·프로필 0경기·룸 게임 0(호스트/참가자 문구 차이) 확인. 에러 경계: 없는 방 → `내려간 매칭입니다`(레이아웃 유지), 없는 userId·타인 기록 `/edit` → 404(레이아웃 유지, CTA 클럽). 다크/라이트 토글은 클래스 `dark`/`light` 전환·새로고침 유지만 확인(대비는 육안 미검) |

### Phase 5 — 탈퇴 A9

| 시나리오 | 결과 | 비고 |
|---|---|---|
| A9 | PASS(FAIL 2) | e2e1(S6 참가 + J1 게임 제안 중)로 [회원 탈퇴] → 팝업 문구·[취소] → [탈퇴하기] → `/login`. **가드 없음** — 진행 중 게임이 있어도 탈퇴된다. SQL 익명화 12컬럼 전부 확인(`ntrp`는 남는다), `auth.users` 잔존(이메일 원본), 클럽 0, 멤버십은 `joined`로 남음. 옛 아이디 → `아이디 또는 비밀번호가 올바르지 않습니다.` / 옛 이메일 → `탈퇴한 계정입니다.` / `/find-id` → 없음. J1 좌석이 [A]로 줄어 A 혼자 확인 → 정산(탈퇴자 분모 제외 실증). 룸 명단 `탈퇴한 회원(탈퇴) · 참가`, 룸 행 `vs 탈퇴한 회원`·`탈퇴: 탈퇴한 회원`, 팝업 `탈퇴한 회원님이 제안한 결과` — **그러나 A의 개인 카드는 `vs 테스트가입자`(원래 이름)**(F-25 표시 불일치). **탈퇴자 프로필 URL**: `탈퇴한 회원 · 남 · 오른손잡이 · NTRP 2.5` — null이 된 성별·주력손이 기본값으로, 남은 ntrp가 사실처럼 보인다(F-pre-8 판정 → P3, 공백이 아니라 거짓 값). e2e2 가입에 **옛 닉네임·아이디 재사용 가능**, 옛 이메일은 `이미 가입된 이메일`. e2e2 탈퇴도 성공(두 번째 탈퇴자 — 부분 유니크 실증). 9.10 구글 탈퇴 차단은 **사용자 항목** |
| 정리 | PASS | 태그 정리 SQL이 **닫힌 방의 트리거(`room_closed`)에 막혀** 먼저 `closed_at = null`을 풀어야 했다(README 반영). 세션 삭제에 `court_name like 'E2E-%'` 추가. 결과: rooms 0 · matches 0 · requests 0 · sessions 0 · e2e 회원 auth/public 0 · A~F 멤버십 0·개인 기록 0 · 고아 세션 0 |

### 마감 (Phase 0~5)
- 결과: A0~A9 · S0~S15 · R1~R5 · J1~J3 전부 실행. SKIP: A2.7(대시보드 최소 길이 미설정) · A8.6(병합 대상 없음) · A9.10 구글 탈퇴(사용자 항목) · S15.8 20명 초과 · S8.10 정렬. 기대값 수정 12건(절차서 반영).
- 결함: **P1 1**(F-15) · **P2 5**(F-19·F-21·F-22·F-24·F-pre-4) · P3 8(F-16·17·18·20·23·25·26·F-pre-8) · 기지 confirmed 2(K-3·K-13) · known 1(F-pre-9=K-9). UX 관찰 U-1~U-14 + U-pre-1~4. 수정 계획은 `findings.md` 「수정 계획 요약」(승인 대기).
- 회귀 확인: Week 49·50 결함 14건 전부 fixed 유지, 0075·0077·0078·0082·0083 동작, 협상 전이 전수·좌석 만장일치·탈퇴자 분모·동시성·라운드 파생·배치 memberRule·뱃지 항등식(F-21 조건 외) 정합.
- 정리: 태그·회원 정리 SQL 뒤 rooms 0 · matches 0 · requests 0 · sessions 0 · e2e 회원 0 · A~F 멤버십 0·개인 기록 0 · 고아 세션 0. 테스트 계정 변경분: C `login_id=namja03`, D 시작일 `2019-12-01`(README 반영), A 원복.
- 도구 메모: 오래된 탭·팝업이 남은 페이지에서 click·keyboard 미도달이 잦다 → 배치마다 `context.newPage()`, base-ui Select는 키보드(click → ArrowDown×n → Enter). `page.on('dialog')` 중복 등록 금지. 브라우저가 멈추면 `browser_close` 뒤 다시 navigate(세션 유지).

## 2026-09-16 · Week 63 재실행 (수정 6묶음)

도구: Playwright MCP·Chrome 확장 모두 닿지 않아(전자는 연결 끊김, 후자는 이 앱에서 스크립트 주입 타임아웃) **npx 캐시의 `playwright` 패키지 + 설치된 Chrome(`channel: 'chrome'`, headless)을 node 스크립트로** 돌렸다. 로그인은 폼 제출, 픽스처는 `execute_sql`로 RPC 직접 호출(사용자 컨텍스트), 정리는 README 규약 SQL. 스토리지 객체는 SQL로 못 지운다(`protect_delete`) — supabase-js로 본인 로그인 후 `storage.remove`.

| 묶음 | 행 | 결과 | 메모 |
|---|---|---|---|
| ① | A6.6 | PASS | 11MB → `사진이 너무 큽니다. 10MB 이하로 골라 주세요.` + [저장하기] 비활성 + input 비움 / 6MB PNG → 고르는 즉시 512px webp **167KB** → 저장 → `profile_image`가 버킷 URL, `storage.objects` size 167444 · mimetype image/webp / 1.5MB → 168KB / [기본 이미지로 변경]은 고른 파일을 버린다 |
| ① | A1 | PASS(→**F-27**) | 첫 실행에서 `사진을 올리지 못했습니다` — **버킷이 없었다**. 0087 적용 뒤 6MB 사진으로 가입 성공, 스토리지 URL 저장 |
| ② | A8.5 재현 | PASS | 로그아웃 상태로 열어 둔 `/login` → 다른 탭 로그인 → 옛 탭 제출 → 프로필 착지, 오류 없음. 보호 화면에서 쿠키 삭제 뒤 [저장하기] → `로그인이 필요합니다`(사람 말) |
| ② | A0.6 · A1.13 | PASS | 로그인 상태 `/find-id`·`/forgot-password` → 프로필. 가입 착지 `/profile/<uid>?scope=personal&notice=welcome` + 배너 + 체크리스트 |
| ③ | S4.13 | PASS | 호스트가 requester가 아닌 대진(team1[0]=B) 전부 확정 → 뱃지 **1** + 카드 필 `게임 입력 종료` + 배너 정합. 롤백 스모크: RLS 직접 select 1행 vs `room_game_tallies` 2/0, 비멤버 0행 |
| ③ | S13.10 | PASS | 비노출 방 거절 → 호스트 [회원 초대] 검색에 `남자02` 후보 → 선택 → `초대 대기` → B 「나를 초대한 매칭」 → 수락 → `참가 2명`. 스모크: 참가자 재초대 0·호스트 1 |
| ⑤ | A7.2 | PASS | 같은 비밀번호 → `기존 비밀번호와 다른 비밀번호를 입력해 주세요.` |
| ⑤ | A1.12 | PASS | 닉네임 충돌 거절 뒤 라켓 기타 `프린스`·모델·동의 체크 보존, 닉네임 칸 아래 `이미 사용 중인 닉네임입니다.` **하나**(공통 줄 없음) |
| ⑤ | S2.4 · S2.6 | PASS | disputed 행 배지 하나 — B `내가 이의 제기` / A `테스트가입자님 이의`, 상태 배지 `이의 제기` 없음 |
| ⑤ | A9.8 | PASS | 탈퇴 뒤 A의 개인 카드 `vs 테스트가입자` + `탈퇴` 배지, 룸 게임 행도 스냅샷 이름 + 배지, 명단 `탈퇴한 회원(탈퇴)`, 프로필 URL → 헤더 `탈퇴한 회원` + 배지 + `탈퇴한 회원입니다.`(성별·주력손·NTRP 줄 없음) |
| ⑤ | R1.2 | PASS(S) | 롤백 스모크: 게임 0 로테이션 방 삭제 → 세션·참가자 0행 / 세션에 게임이 있는 방 → 세션 잔존 `room_id null` |
| ④ | R4.6 | PASS | B가 A(1승 1패) 프로필 → 헤더 승률 링·주력 단식·개인 레이팅, 4카드 = A 본인 값, 라이벌·NTRP 대비·최근 개인 경기(`vs 남자02`) 있음, 공개 토글·[+ 직접 기록] 없음. A 비공개 → 잠긴 4카드 + `승률을 공개하지 않은 유저입니다` |
| ⑥ | A1.2 · A6.3 · A6.2 · S1.11 · S3.5 · S4.2 · A2.8 · R5.1 | PASS | `ABC_1` → `abc_1` / 잘못된 휴대폰 동안 [저장하기] 비활성, 저장 후 `저장했습니다.` / invited B에게 [매칭 나가기] 없음·[거절]만 / 헤더 `참가 1명 · 비회원 1명` / 게임 0 로테이션 방에 [게임 입력 종료] 없음 / 「로그인 상태 유지」 삭제 / 로고 → 프로필 |
| 정리 | PASS | 방·세션·요청 0, e2e 회원 0, 스토리지 객체 0. A: `stats_hidden=false` 원복, phone은 null(원래 값 없음) |
| **M** A9.10 | PASS | 사용자 조작(구글 계정 pangnima): 「내 정보 수정」 [회원 탈퇴] → `/login` → [Google로 계속하기] → 「탈퇴한 계정입니다.」 배너. SQL: 10:51:29 익명화(`탈퇴한 회원`, `personal_ntrp` null, `ntrp` 2.0 잔존 — 게이트 술어), 1.9초 뒤 `last_sign_in_at` 갱신 + 살아 있는 세션 0(콜백이 끊음). 이후 두 행(auth·public) SQL 삭제 |
| **M** A8 재가입 · U-6 · F-pre-4 | PASS | 새 uuid `83f158b3…`. 완성 화면 버튼 「회원가입」, 이름 `J Pi` 프리필, NTRP 3.5·남·오른손·2022/02·요넥스·휴대폰 저장. 착지 `?notice=welcome` + 배너 「가입이 완료됐습니다.」 + 체크리스트 「1단계 완료」(휴대폰·라켓 입력 → 프로필 단계 done, U-pre-1). 뒤로 가기 두 번 뒤 구글 재로그인 오류 없음(F-19). 사진은 `/avatars/avatar-sunset.png`(무작위 기본 아바타) — [다른 기본 이미지]를 눌렀는지 확인 대기 |
| **M** A6.6 구글 계정 | PASS | 「내 정보 수정」 [이미지 변경] 6MB PNG → 「줄이는 중…」 → 저장 → `avatars/83f158b3…/avatar.webp` **167444B** image/webp, `profile_image`가 버킷 URL(Week 56 잔여 ①의 설정 화면 크래시 없음도 함께) |

## 2026-09-16 · Week 64 회원 검색 재설계 재실행

도구: Playwright MCP(`browser_run_code_unsafe`) + `execute_sql` 픽스처(멤버 status 직접 갱신). 방 `E2E-S16`(A 호스트, 단식), 정리 SQL로 0건 확인.

| 행 | 결과 | 메모 |
|---|---|---|
| 1.4 · 15.8 | PASS | 매칭 만들기 `남` 1자 + Enter → 「검색 중…」 → 결과 20행 + `20명까지만 보입니다…`, **URL 그대로**(폼 제출 없음). 행 클릭 → 칩 `남자02`·`남자03`, 같은 행 재클릭 → 칩 제거·`aria-pressed=false`. `없는사람zzz` → `'없는사람zzz'와 일치하는 회원이 없습니다.`, 칩은 남는다. 390px 가로 스크롤 없음 |
| 1.5 · 1.12a | PASS | 칩 `남자03`으로 방 생성 → 명단 `남자03 초대 대기`. [회원 초대] → idle 문구 → `남자0` Enter → `남자03` 비활성 + 칩 `초대 대기`, `남자02`·`남자04` 클릭 → 버튼 `0명 초대하기`(비활성) → `2명 초대하기` → 팝업 닫힘 → 명단 `초대 대기` 3행(02·03·04). 재검색 `남자02` → 비활성 `초대 대기` |
| 6.6 · 13.10(참가자) | PASS | SQL: 02 joined · 03 declined · 04 removed. **B(참가)** 검색 `남자0` → `남자01` `호스트`·`남자03` `나감`·`남자04` `내보내짐` 전부 **비활성**(감추지 않음, K-2), `남자05` 활성 |
| 6.7 · 13.10(호스트) | PASS | **A** 검색 → `남자02` `참가` 비활성, `남자03` `나감` **활성**, `남자04` `내보내짐` **활성** → 04 클릭 → [1명 초대하기] → 명단 `남자04 초대 대기`(removed→invited) |
| 15.9 | PASS | 본인 `남자01`은 호스트 화면 결과에 없음(쿼리 `neq self`), 참가자 화면에서는 `호스트` 비활성 |
| S3.2(선수 입력) | PASS | [게임 추가] 상대 칸 `김` 1자 → 팝업 `[role=status]` 「검색 중…」 → 없으면 기존 Empty 문구 / `남` → 「매칭 참가자 남자02」 + 「전체 회원 남자03…」 |
| 정리 | PASS | 방·기록·요청 0 |

## 2026-09-16 · Week 65 랜딩 재설계 확인

도구: Playwright MCP(`context().clearCookies()`로 비로그인) + Chrome 확장(로그인 세션). 룸·협상 시나리오는 건드리지 않아 재실행 없음.

| 항목 | 결과 | 관찰 |
|---|---|---|
| 비로그인 `/` 1400px | PASS | 다크 고정, 옐로우 글로우·[회원가입 ↗] accent 2곳(히어로·CTA) + 나브 [로그인] outline, 콜라주(매칭 카드 2장 + 확정 카드 + 필 3개)가 카피와 겹치지 않음(첫 시도에서 확정 카드가 첫 카드 제목을 덮어 `lg:-top-24 lg:right-0 lg:w-72`로 조정, 글로우는 루트가 스태킹 컨텍스트가 아니라 `-z-10`이 배경 뒤로 숨어 `isolate` 추가) |
| 390px | PASS | 카피·버튼·필은 중앙 정렬(카드 안은 text-left로 실제 화면과 같게), 글로우를 컨테이너 폭으로 제한해 가로 스크롤 0(scrollWidth = clientWidth). 카피 → 카드 2장 → 확정 카드 → 필 3개 wrap → 흐름 1열 → 기능 1열 → CTA → 푸터(© 2026) |
| inert | PASS | Tab 순서 로고 → 흐름 → 기능 → 사용 가이드 → 로그인(나브) → 회원가입(히어로) → 사용 가이드 — 카드 3장의 `/match-rooms/…` 링크 건너뜀 |
| 링크 | PASS | `href` 집합 `/ #flow #features /guide /signup /login` + 픽스처 방 3개(inert). `href="#"` 0 |
| 로그인 상태 `/` | PASS | Chrome(구글 계정) → `/profile/83f158b3…?scope=personal` 리다이렉트 유지 |
| 콘솔 | PASS | 오류·경고 0 |
| 빌드 | PASS | `/` ○ Static(getUser 제거) · tsc · lint(기존 경고 2) · vitest 1029 |

## 2026-09-16 · Week 66 채움 버튼 색 통일 확인

채움(solid) 버튼·CTA를 전부 옐로우(`spot-solid`)로 — Button `variant="accent"` 26곳 + 인라인 `bg-primary` CTA 10곳을 `CTA_LINK`로, [클럽 만들기]·[대진표 만들기]는 `HEADER_ACTION_LINK`(테두리 칩)로. 룸·협상·직접 기록 로직은 건드리지 않아 시나리오 재실행 없음(색·클래스만).

| 항목 | 결과 | 관찰 |
|---|---|---|
| 정적 검사 | PASS | tsc · lint(기존 경고 2) · vitest 1029 · build |
| 잔여 grep | PASS | `bg-primary text-primary-foreground` → `member-result-row`(선택 체크)·미사용 `chip.tsx`뿐. `<Button` 단일/다중 줄 전부 variant 명시, `buttonVariants()` 무variant 0 |
| 브라우저(Playwright MCP, 남자01) | PASS | 비로그인 `/login`·`/signup`·`/find-id`·`/forgot-password`·`/`·`/guide`·404, 로그인 후 프로필(빈 상태 CTA 3)·`/profile/settings`([저장하기]·[비밀번호 변경])·`/me/match-rooms`·`/match-rooms`·`/me/personal-matches`·`/match-rooms/new`·룸 게이트 [입장] — 채움 버튼 computed bg 전부 `rgb(255,209,102)`, `bg-primary` 채움 0. 두 목록 헤더의 [+ 매칭 만들기]·[+ 직접 기록]은 테두리 칩 유지. 라이트 토글(`dark` 클래스 제거)에서도 같은 옐로우 |

## 2026-09-16 · Week 67 사이드 메뉴 IA·명칭 재정비 확인

계정 A(남자01, 세션 유지). Chrome 확장은 주입 타임아웃(기록된 증상)이라 Playwright MCP로.

| 항목 | 결과 | 관찰 |
|---|---|---|
| 정적 검사 | PASS | tsc · lint(기존 경고 2) · vitest 1031(신규 `nav-items.test.ts` 2 + `sections.test` RETIRED_WORDS) · build |
| 잔여 grep | PASS | `rg '개인 경기 결과' src` → `nav-items.ts` 이력 주석 1건뿐(가드는 주석을 벗기고 본다). `label: '개인'`은 `profile-scope-tabs.tsx`(통계 범위 탭)만 |
| 모바일 시트(390px) | PASS | 최상단 옐로우 [+ 매칭 만들기] → 개인 통계·매칭 리스트·참여 중인 매칭·내 경기 결과 | 사용 가이드. CTA 클릭 → `/match-rooms/new` 착지 + 시트 닫힘(`role=dialog` 0) |
| 데스크톱 펼침(1400px) | PASS | 사이드바 최상단 full-width 옐로우 CTA, 5항목 순서·활성(`/me/personal-matches`에서 「내 경기 결과」). `/me/personal-matches` [+ 직접 기록]이 헤더 actions에서 PageGuide 아래 우측 행으로 |
| rail(접힘) | PASS | CTA가 w-10 h-10 옐로우 정사각 + 아이콘만, `aria-label="매칭 만들기"`, 텍스트 없음 |
| 프로필 본인 | PASS | `MemberProfileHeader` 위 eyebrow 「개인 통계」. scope 탭 '개인/클럽/통합'은 그대로 |
| `/guide` | PASS | h2 「매칭 리스트」·「참여 중인 매칭」·「내 경기 결과」 + 「… 열기」 CTA, FLOW 4단계 "내 경기 결과와 개인 통계에 반영", 스테퍼 4칸 「전적으로 내 경기 결과」, 다섯 단계 종료 힌트 "전적은 내 경기 결과에서" — 전부 `NAV_LABEL` 보간 |
| 비로그인 `/guide` | PASS | 사이드바에 CTA 없음, 「사용 가이드」 한 줄 + 헤더 [로그인] |
| 미실행 | — | 타인 프로필(eyebrow 없음 — 코드 분기 `isSelf`만 확인), 정산 방 안내의 「내 경기 결과」 링크(정산 방 픽스처 없음 — 문자열 보간은 가이드 STAGES로 대신 확인) |

## 2026-09-16 · Week 68 환경 분리 확인 (dev / prod)

| 항목 | 결과 | 관찰 |
|---|---|---|
| 스키마 재현 | PASS | `supabase/history/` 119건 → prod `rjuhydxaoizgfiatyfpo` replay(schema_migrations에 원본 version·name), seed `admin@tennis-club.com` 삭제. 정의 스냅샷 diff — routine_grants 4건(트리거 함수 3 PUBLIC/anon/authenticated · `update_match_room_password` anon)만 달라 `0091_revoke_execute_drift`로 dev·prod 적용 → **diff 0**. `generate_typescript_types`(prod) = `types/supabase.ts` diff 0 |
| prod Auth 설정 | PASS | `/auth/v1/settings`: `external.email=true`·`external.google=true`·`mailer_autoconfirm=true`·`disable_signup=false`. 중간에 Email provider 자체가 꺼졌던 것(`external.email=false`)과 Confirm email이 남아 있던 것을 두 번 되돌렸다 |
| Vercel Production env | PASS | `baselineplay.vercel.app/signup` 닉네임 중복 확인 RPC가 `rjuhydxaoizgfiatyfpo.supabase.co`로 나간다(재배포 전에는 dev로 나갔다) |
| 구글 로그인(M, 사용자) | PASS | 첫 시도는 localhost로 착지 — prod URL Configuration이 기본값(Site URL localhost, Redirect URLs 비어 있음)이라 allowlist 폴백(Week 59 재현). Site URL·Redirect URLs 등록 후 auth_logs `/authorize` referer `…/auth/callback` 전체 경로 → `/token pkce 200`. 프로필 완성(NTRP 3.0) → `role='admin'` 승격 |
| prod 데이터 | PASS | auth.users 1 · public.users 1 · rooms/pm/sessions/requests 0. 테스트 계정 0 |
| 미실행 | — | 매칭 만들기 → 내리기 · 회원 검색 "남자" 0명(사용자 수동 — 핵심 검증과 무관해 생략 가능). Google Cloud는 **새 프로젝트**의 OAuth 클라이언트라 동의 화면이 테스트 상태 — 오픈 전 게시 필요 |
